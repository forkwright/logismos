[package]
name = "decoders"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "Decoder-only family boundaries, including Qwen3.5 structural preflight and digest-verified executable row projections."
publish = false

[lints]
workspace = true

[dependencies]
# Metadata-only observation keeps structural preflight independent of HIP/tensor decoding.
loader = { path = "../loader", default-features = false }
kernels = { workspace = true, default-features = false }
num-traits = { workspace = true }
quant = { path = "../quant" }
snafu = { workspace = true }

[dev-dependencies]
sha2 = { workspace = true }
tempfile = { workspace = true }
test-fixtures = { path = "../test-fixtures" }
