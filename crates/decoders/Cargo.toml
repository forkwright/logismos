[package]
name = "decoders"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "Decoder-only family boundaries, including Qwen3.5 structural preflight and one digest-verified Q8_0 projection."
publish = false

[lints]
workspace = true

[dependencies]
# Metadata-only observation keeps structural preflight independent of HIP/tensor decoding.
loader = { path = "../loader", default-features = false }
quant = { path = "../quant" }
snafu = { workspace = true }

[dev-dependencies]
sha2 = { workspace = true }
tempfile = { workspace = true }
