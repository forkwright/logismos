[package]
name = "hipcore"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "HIP FFI + device / stream / event / memory primitives. Foundation of the logismos platform."
publish = false
links = "amdhip64"

[dependencies]
gpu-target.workspace = true
half.workspace = true
snafu.workspace = true

[build-dependencies]
bindgen.workspace = true

[dev-dependencies]
tempfile.workspace = true

[lints]
workspace = true
