[package]
name = "bin"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "logismos placement-planning, GGUF-inspection, and text-preparation CLI with CPU-only commands."
publish = false

[dependencies]
decoders = { path = "../decoders" }
placement = { workspace = true }
loader = { path = "../loader", default-features = false }
serde = { workspace = true }
serde_json = { workspace = true }
text = { workspace = true }
tokenize = { workspace = true }

[dev-dependencies]
sha2 = { workspace = true }
test-fixtures = { path = "../test-fixtures" }
tempfile.workspace = true

[[bin]]
name = "logismos"
path = "src/main.rs"

[lints]
workspace = true
