//! # logismos binary
//!
//! Single binary entrypoint. Loads whichever model family is named
//! and serves via `hermeneus` (or, for one-shot CLI use, embeds /
//! classifies / transcribes directly).
//!
//! Phase 0 scaffold. No functional code yet.

fn main() {
    // Phase 0 scaffold; replaced in Phase 7.
    println!("logismos (Phase 0 scaffold — no runtime yet)");
}
