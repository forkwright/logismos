[package]
name = "praxis"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "Composed high-level ops: matmul, norms, softmax, attention, rotary, SwiGLU. Greek πρᾶξις, action."
publish = false

[dependencies]
half.workspace = true
hipcore.workspace = true
# Praxis owns the existing launch-path composition and must select it explicitly.
kernels = { workspace = true, features = ["gpu"] }
num-traits.workspace = true
taxis.workspace = true
snafu.workspace = true

[dev-dependencies]
approx.workspace = true
rand.workspace = true

[lints]
workspace = true
